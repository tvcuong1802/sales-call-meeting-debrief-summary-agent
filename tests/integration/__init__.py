# tests/integration/ — CMN-C1-042
# Integration tests run against a real (or realistic stub) LLM client.
# Requires: OPENAI_API_KEY or compatible LLM client configured in config/config.yaml.
# Run separately from unit tests: pytest tests/integration/ -v
